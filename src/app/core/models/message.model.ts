export interface MessageModel {
   
   
   title:string, //Dialog title
   msg:string, //genericMessage
   duration:number, //snackBAr duration
   dataObject:string, //aditional data to send
   horizontal:string, //end-0, start-0
   vertical:string, //bottom-0, top-0
   active:number, //0 or 1
   icon:string
}
